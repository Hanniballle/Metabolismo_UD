import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  cssVariables: true,
  palette: {
    primary: {
      main: '#2a7fff'
    },
    secondary: {
      main: '#01609C',
    }
  },
});

export default theme;
